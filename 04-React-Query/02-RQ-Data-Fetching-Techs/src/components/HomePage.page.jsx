export const HomePage = () => {
    return <>
        <h3>React Query - now Tanstack React Query</h3>
        <ul>
            <li>Use <code>npm i @tanstack/react-query</code> to install the latest version of React Query.</li>
            <li>React query package installed with <code>npm i react-query</code> was the older version uptil version 3.</li>
            <li>Author of React Query chose to release latest versions with <b>tanstack query</b>.</li>
        </ul>
    </>
}

export default HomePage;